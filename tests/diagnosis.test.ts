import { describe, expect, it } from "vitest";

// Placeholder tests to keep suite green while diagnosis module is absent

describe("diagnosis placeholder", () => {
  it("runs a trivial assertion", () => {
    expect(1 + 1).toBe(2);
  });
});
